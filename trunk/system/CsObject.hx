package system;

//haxe does not have an "object" type like C# does.  So the statement "new object()" will end up creating a CsObject.  This is only useful for locking.
class CsObject
{

	public function new() 
	{
		
	}
	
}